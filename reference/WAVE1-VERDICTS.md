# WAVE 1 CRITIC VERDICTS — final round per file (captured 2026-07-29, run wf_1dbf8562-6c8)

Source: hostile fresh-context critics, 3-round gate. All ten scored `forced_choice: THIS_FILE_WINS`
(the content beats a smart operator holding only the baseline prompt). Failures are internal
correctness and cross-file contract drift, not weak content. Fix these in Wave 2.

### README.md  —  round 3: **FAIL** (0 blocker / 3 major)
**Largest gap:** The headline promise misstates how the loop actually ends. Line 3-5 promises a loop "that stops only when a fresh-context critic... picks yours" â but the system's own doctrine says that outcome is rare and is NOT the exit: LAUNCH.md Â§2 states "The emission cannot terminate on its own. By construction the bar is out of reach," and OPERATIONS.md Â§5 labels marginal-gain collapse "The normal exit." The README itself concedes this at line 35 ("The bar is out of reach by construction, so with no stop rule the loop cannot end"). So the single most-read sentence in the entry point sells the one termination path that almost never fires, and hides the one that does. Since mis-set termination expectation is, per this very file, "the one way to lose money on a good prompt," getting it wrong in the promise sentence is the most expensive error available here.

**Ship test:** Not yet, though it is close. A top practitioner would recognise the density, the verified cross-references, and the honest 'use the baseline instead' carve-out as their own work â but they would not ship a file whose first sentence promises a termination mode their own doctrine calls rare, and whose first command tells the generator to swallow 356 lines of prescription the system's thesis says degrades it. Two errors in the two lines every reader hits first is a redraft, not a nit list.

### DOCTRINE.md  —  round 2: **FAIL** (0 blocker / 4 major)
**Largest gap:** Termination theory â the section that decides when an operator stops spending â is stated in statistical terms that nothing in this repo implements and that the repo's only implementation directly violates. DOCTRINE:352 says "bar crossing is unprovable at n = 5 for any target below 100%" and DOCTRINE:379 lists "a win rate computed on fewer judgments than your alpha needs, in either direction" as an illegitimate stop; OPERATIONS.md:197 fires bar crossing at `verdicts.length >= 5 && barWins / verdicts.length >= TARGET` and OPERATIONS.md:210-217 declares collapse from a 5-judge panel via `GAIN_THRESHOLD = {5: 5, 7: 6, 9: 7}` plus `dry >= 2`. DOCTRINE gives rule (a) a five-row exact binomial table but gives rule (b) â which it calls "the normal exit" â no table, no n, no interval formula, only "the confidence interval on the new-vs-previous win rate contains 50% AND its upper bound sits below your minimum-interesting effect." So the one stopping rule almost every run will actually use cannot be computed from this file, is not implemented anywhere in the system, and the file never adjudicates the conflict with the harness it points readers to. The theory does not cash out in an operator behaviour at the exact point where it costs money.

**Ship test:** Yes on substance, no on release-readiness. The evidence-channel argument, the verified-exact incident forensics, the correct binomial power table, and the calibration probe that doubles as the round-0 baseline are all senior-specialist work that a competent operator with the baseline prompt would not produce â but four of the file's operative rules are wrong or unusable as written (the noun test condemns the product's own emissions, the re-open receipt readmits bar drift, and both stopping rules conflict with the only implementation in the repo), and a practitioner would not put their name on an audit instrument that fails when pointed at its own system.

### LAUNCH.md  —  round 3: **FAIL** (1 blocker / 2 major)
**Largest gap:** The STEP 2b audio row â copied verbatim into every voice/phone emission â instructs the critic to "judge the recording itself" and example 5b's forced choice tells it to compare two calls "matched for loudness, and make it pick", but the repo's own `INSPECTION.md` Â§9 opens with "State the limit first: **the critic cannot hear.**" The flagship audio emission therefore commands an inspection the harness cannot perform, which produces exactly the confident fabricated verdict this file's Â§2 table names as "the failure that killed a real run." The file fails its own central test in one of its three showcase examples.

**Ship test:** No. The bar interrogation, the access-path verification with PROXY fallback, and the modality router are genuinely strong work that beats a smart operator holding only the baseline â but a voice-AI practitioner would reject this file on sight for shipping an audio row and a flagship audio example that order a deaf critic to judge and blind-compare recordings, when the repo's own INSPECTION.md states that limit in its first line. Add the 40x contradiction in the STOP token arithmetic against install/SKILL.md and this is a file whose two most consequential operator-facing outputs â the modality phrase and the ceiling â are both wrong for at least one whole domain.

### BARS.md  —  round 1: **PASS** (0 blocker / 4 major)
**Largest gap:** BAR ESCALATION's trigger is the same observed event as FAILURE-MODES F20's termination condition, and the two mandate opposite actions. BARS.md L410: "Ours wins the blind pick in 2 consecutive rounds with different critic instances" â escalate the bar, L415 "A milestone, not a reset", L419 "Expect the first round under a new bar to lose." FAILURE-MODES.md L310-312: "the candidate wins the blind pairwise choice against the bar in two consecutive rounds with sides swapped" â terminate. An operator holding BARS.md alone can never exit: every clearing event is converted into a new bar epoch and an expected loss. The two conditions are also not identical ("different critic instances" is weaker than "sides swapped"), so the same round can satisfy one and not the other.

**Ship test:** Yes. The blinding mechanics (proxy-fidelity degradation, loudness normalisation, retyped table rows, read-aloud, 16px favicon in the pair, hook-only-first judging) and the ANTI-BAR entries are things practitioners learn by losing runs, not by reading, and no competent operator with only the 120-word baseline would arrive at Things 3 over the HIG, jaffle_shop over a dashboard, a Trail of Bits report as the bar for the review artifact, or their own best rep's recording for voice. The four defects I found are one-line fixes that leave the structure and content intact â a specialist would send this back for line edits, not a rewrite.

### ROLES.md  —  round 1: **FAIL** (2 blocker / 4 major)
**Largest gap:** The ANTI-CAPTURE section â the file's own named anti-capture mechanism â is the one enforcement surface in this repo with no executable mechanism, and one of its seven steps cannot be executed at all on the harness the repo recommends. Step 2 tells "a workflow author" to assign A/B by "a fresh coin flip per round" (and the ORCHESTRATOR block repeats "flip a coin for which is A"), but OPERATIONS.md Â§1 states as a verified harness constraint that `Math.random()`, `Date.now()` and argless `new Date()` **throw** inside a Workflow script, and gives the canonical replacement `const candAt = (r, i) => ((r + i) % 2 === 0 ? 'A' : 'B')`. FAILURE-MODES.md and OPERATIONS.md Â§6 both build their side-mapping and F11 checks on `candAt`. ROLES.md, the file that owns the blind-comparison prompts, therefore specifies an unimplementable shuffle; and step 1's "assert the assembled prompt differs from the template only in slot positions" has no command, snippet, or check behind it, in a repo where every sibling ships runnable bash or JS for its enforcement claims.

**Ship test:** Mostly yes on craft, no on shipping state: the prose is dense, the forbidden-knowledge lists and the critic's blindness clauses are the kind of thing only someone who has watched a critic get captured writes, and there is no filler to speak of. But a senior specialist would not put their name on a \"verbatim spawnable prompts\" file in which one of five blocks is aimed at the wrong artifact, another cannot be slot-filled, the enforcement procedure prescribes a shuffle the recommended harness throws on, and the critic's output contract contradicts the sibling file that owns probes.

### INSPECTION.md  —  round 2: **FAIL** (2 blocker / 2 major)
**Largest gap:** The closing "Verdict contract" section contradicts ROLES.md, the file that owns the critic schema, and does it on the two fields that carry blindness. It claims its four additions are "absorbable verbatim" (line 377), then (a) redefines the required field `not_probed` as an array of objects `{blind_spot, covering_recipe}` when ROLES.md line 244 already defines it as an array of strings â a critic obeying INSPECTION emits JSON that fails ROLES' `schema` validation, and per OPERATIONS.md Â§1 "a null verdict is a FAIL, never an absent one", so the round burns and voids; and (b) introduces `order_swap_recheck` with enum `["same","flipped","not_required"]` "required on termination-deciding rounds", when ROLES.md line 246 names the same concept `order_swap`, has the critic always write `not_run`, and lists under Forbidden knowledge (line 219) "the round number; whether a swapped twin of this judgment exists". Filling INSPECTION's field requires the critic to know both. The file that exists to enforce modality-matched blindness ships a field that breaks blindness, and its own audit `jq` (line 399) reads a key ROLES' critic will never emit, so it prints nulls forever and the F11 check silently passes.

**Ship test:** Yes for the twelve recipes and the parity section â the reduce/split/label move is a genuinely original closure of the evidence channel DOCTRINE leaves open, and the gotchas (curl `--path-as-is` or the traversal probe tests nothing, zero-padding or the tile sheet lies, two separate `javascript_tool` calls because 600 rAF frames outlive one serialized return, the 4pt iOS edge gesture, `n===600` or the sample is void) are the exact knowledge a specialist would otherwise pay for in wasted rounds, and every tool and CLI claim I checked on this machine is true including the ABSENT list. No for the file as shipped: a practitioner would not sign a document that redefines a sibling's required schema field with an incompatible type, renames the blindness field so the audit reads nulls, and asserts a change to FAILURE-MODES.md that FAILURE-MODES.md does not contain.

### FAILURE-MODES.md  —  round 2: **FAIL** (1 blocker / 4 major)
**Largest gap:** F20's termination rule licenses a stop at n=2. "MITIGATION: â¦ (a) the candidate wins the blind pairwise choice against the bar in two consecutive rounds with sides swapped" (line 299-300) is two judgments, p=0.25 against a fair coin. DOCTRINE.md line 379 names "a win rate computed on fewer judgments than your alpha needs, in either direction" an illegitimate stopping rule that "has ended a real loop early," its table shows n=5/k=5 as the smallest result that clears pâ¤0.05, and it states bar crossing is "unprovable at n = 5 for any target below 100%"; OPERATIONS.md line 197 refuses to stop below `verdicts.length >= 5`. So the one file an operator reads mid-run, whose triage table is the ten-second diagnostic, prescribes the exact stopping rule the doctrine forbids â and F11's mitigation repeats it ("for any round that decides termination, judge twice with sides swapped"). A file whose job is catching invariant violations violates I5 in its own mitigation text.

**Ship test:** No. The taxonomy, the JSON-first record design, and the decision to make most signals structural rather than lexical are senior-specialist work, and the coverage is complete with five well-justified additions beyond the brief â but a practitioner would not sign a failure-mode catalogue whose own termination mitigation is the stopping rule the doctrine file calls illegitimate, and whose flagship F2 signal cannot fire because a sibling mitigation guarantees the strings never repeat. Those are not nits; they are the two things this file exists to get right, and the second one makes the round-close audit report all-clear on a thrashing run.

### OPERATIONS.md  —  round 2: **FAIL** (3 blocker / 2 major)
**Largest gap:** Â§5 Termination policy â the one section the whole system's doctrine hangs on â is arithmetically and mechanically wrong in three independent ways, all of which fail toward stopping early or never stopping on evidence: the GAIN table does not satisfy the p â¤ 0.05 test its own comment claims, an unguarded `GAIN[deltas.length]` turns any unlisted panel size into a fixed 2-round counter, and `candAt(r, i)` is called with the judge index where the rest of the repo defines `i` as the part index, so a clean 10â0 bar crossing computes as 5 and rule (a) can never fire.

**Ship test:** Yes on craft and no on this draft: the harness knowledge here is first-rate and demonstrably real â I re-ran its jq lines against the run records and the 2.31M-token round, the 318k/82-call/43-minute critic, the null-`durationMs` truncation trap and the session-scoped workflows path all check out to the digit, which is more verification than almost any runbook carries. But a senior specialist would not ship a stopping rule whose own comment is arithmetically false, whose lookup silently converts a 15-judge panel into a 2-round counter, and whose win-counting filter makes a 10â0 crossing read as 5â5 â the file's central gate is unrunnable as written, and its own house rule is that a check which silently matches nothing is the worst possible outcome.

### EXAMPLES.md  —  round 2: **FAIL** (2 blocker / 6 major)
**Largest gap:** The file's centerpiece â brief item 5, "one full sample critic verdict, in the real output contract" â is not in the real output contract, and the file asserts a checkably false claim about that contract to license the deviation. All three sample verdicts (lines 143-159, 298-315, 454-470) emit `not_probed` as a bare string when ROLES.md's schema requires an array (minItems 1) and INSPECTION.md requires an array of `{blind_spot, covering_recipe}` objects; all three omit `recipe_class` and `parity`, both listed as required in INSPECTION.md's "Verdict contract"; and all three have the critic itself returning `order_swap: "same"` when ROLES.md says "a critic always returns [it] as `not_run`, because knowing a swapped twin exists is knowing a prior verdict exists." Every one of these three verdicts would fail `schema` validation in the reference harness, so the artifact a reader is most likely to copy is the artifact that cannot run.

**Ship test:** No. A senior specialist would not ship three \"verbatim, in the real output contract\" verdicts that every one of them would fail `schema` validation on, defended by a false statement about what that schema contains, nor three independent runs reporting the identical panel-judgment total. The bar-interrogation and non-visual-blindness material is genuinely expert work that beats the baseline prompt outright, which is what makes the unforced errors disqualifying rather than excusable â the file is one careful pass away from passing and has not had it.

### install/SKILL.md  —  round 3: **FAIL** (0 blocker / 4 major)
**Largest gap:** The two operator-facing contracts in this file â the stop rule printed in the gate and the `--resume` flag â do not match the sibling files they cite as their implementation. The gate promises stop rule (a) as "blind critics pick ours over the bar in two consecutive rounds with sides swapped" and the Invariants table repeats it, but OPERATIONS.md Â§5(a), which step 6.5 orders the author to follow, implements a completely different rule: a single-round panel of `verdicts.length >= 5` at `barWins/verdicts.length >= TARGET`. So the operator approves compute against a termination criterion the script will not contain. Compounding it, `--resume` claims to "resume the Workflow by `runId` per `OPERATIONS.md` Â§8" while Â§8 opens with "Same session only" â meaning the flag, as advertised in `argument-hint`, cannot work in the situation an operator actually reaches for it (a new session the next morning), and the file offers no alternative path.

**Ship test:** Most of it, yes â the per-step load map, the `allowed-tools` refusal note, and the install failure-signature table are the work of someone who has actually installed and broken skills, and the section cross-references check out against the siblings line by line. But a senior specialist would not ship a gate that prints a stop rule the script it orders will not implement, or a `--resume` flag whose cited implementation section says \"same session only\" â both are contradictions with files this same author's system depends on, and both are one-line fixes, so failing costs a round and buys correctness.
